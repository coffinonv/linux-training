# Use ICMP packet to send echo request to the address
ping <IP-address>

# Display the path that packets take from the system to destination
traceroute <IP-address>

# List all connections with numerical values
netstat -n

# Display packets being transmitted or received over the given interface
sudo tcpdump -i <interface>

# Check running processes
top
